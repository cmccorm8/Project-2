#ifndef _HASHTABLE_H_
#define _HASHTABLE_H_

#include <iostream>
#include <string>
#include <vector>
#include <cmath>
#include <functional>

using std::cin;
using std::cout;
using std::endl;
using std::cin;
using std::cout;
using std::endl;
using std::string;
using std::vector;
using std::hash;

class Container
{
	struct HashTable
	{
	private:
		string ID;
		string Parent_ID;
		string Raw_Event;
		string RHASH;
		string LHASH;
		vector<string> RHISTH;
		vector<string> LHISTH;
	public:
		
	};
};


#endif
