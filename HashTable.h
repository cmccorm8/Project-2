#ifndef _HASHTABLE_H_
#define _HASHTABLE_H_

#include <iostream>
#include <string>
#include <vector>
#include <cmath>

using std::cin;
using std::cout;
using std::endl;

class HashTable
{
	struct hash
	{
	private:
		string ID;
		string Parent_ID;
		string Raw_Event;
		string RHASH;
		string LHASH;
		vector<string> RHISTH;
		vector<string> LHISTH;
	public:
		hash *ptr;
	};
};

HashTable
#endif
